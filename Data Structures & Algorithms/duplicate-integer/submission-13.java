class Solution {
    public boolean hasDuplicate(int[] nums) {
        Scanner sc=new Scanner(System.in);
         int n=sc.nextInt();
        int[] arr=new int();
        for(i=0;i<=n;i++)
        {
         n[i]=sc.nextInt();

        }
        if(a[i]==a[i+1])
        return true;
        else 
        return false;
        
    }
}